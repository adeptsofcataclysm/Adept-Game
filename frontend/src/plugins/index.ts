/**
 * Plugin barrel — auto-generated by scripts/gen-plugin-barrel.ts in CI/prebuild.
 *
 * Each installed @adept-plugins/* package exports `registerClient(registry)`
 * from its `./client` entry; the script imports those and calls them here.
 *
 * This file is intentionally empty (no third-party plugins installed yet).
 * The clientPluginRegistry re-export lets ShowPage and other host components
 * import the registry from a single well-known path.
 */

export { clientPluginRegistry } from "./registry";
