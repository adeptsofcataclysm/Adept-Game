/**
 * Deprecated — superseded by `PluginSegmentLayoutHost`.
 * Kept as a thin re-export so any stray imports keep working; safe to delete
 * once the codebase has confirmed no remaining references.
 */
export { PluginSegmentMainHost as PluginSegmentHost } from "./PluginSegmentLayoutHost";
