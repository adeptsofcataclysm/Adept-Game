/**
 * Client-side views for built-in plugin segments (pluginId "builtin").
 * Registered in index.ts at bundle time via clientPluginRegistry.
 *
 * Segments:
 *   (none — all segments moved to first-party plugins)
 */

export {};
